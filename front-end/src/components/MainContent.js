import React from 'react';

const MainContent = () => (
  <header style={{ padding: '20px', backgroundColor: '#eee', textAlign: 'center' }}>
    <h1>Wireframe Header</h1>
  </header>
);

export default Header;
